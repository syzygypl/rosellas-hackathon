# MCP server
MCP_HOST=0.0.0.0
MCP_PORT=8123

# External Ollama/OpenAI-compatible embeddings API.
# Use the /v1 base URL of a reachable Ollama-compatible service.
EMBEDDING_MODEL=embeddinggemma:300m
EMBEDDING_SERVICE_URL=https://<ollama-host>/v1
EMBEDDING_API_KEY=ollama
